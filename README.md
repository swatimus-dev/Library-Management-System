Library Management System (SQL)
Project Overview

The Library Management System is a database project developed using SQL to manage and organize library records efficiently. The system stores information about books, authors, and issued books in structured tables.

The project demonstrates the use of relational database design and SQL queries to perform common library operations such as adding books, searching for books, issuing books, and returning books.
